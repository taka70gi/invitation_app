
<%= link_to "イベント追加", new_schedule_path %>
<%= month_calendar events: @schedules do |date, schedules| %>
  <%= date.day %>
  <% schedules.each do |schedule| %>
    <div>
      <%= schedule.title %>
    </div>
  <% end %>
<% end %>
